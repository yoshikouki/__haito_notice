%h1.h5.pb-4 ログイン

= form_with(scope: :session, url: login_path,  local: true, id: "login-form") do |form|
  .mb-4
    = form.label :email, "メールアドレス"
    = form.text_field :email, id: "user-email", class: 'form-control'
  .mb-4
    = form.label :password, "パスワード"
    = form.password_field :password, id: "user-password", class: 'form-control'
    .small.text-right.mt-2
      = link_to "パスワードをお忘れの方", new_password_reset_path, id: "password-reset-link", class: "text-muted"
  .form-check.mb-4
    = form.label :remember_me, class: "form-check-label" do
      = form.check_box :remember_me, class: "form-check-input"
      %span ログインを記憶する
  .mb-3
    = form.submit "ログイン", class: "btn btn-primary btn-block"

.my-5
  = link_to "アカウント登録", signup_path
