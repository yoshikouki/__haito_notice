- provide(:title, t('.title'))

.content-title.mb-4
  %h1.h5
    = t('.title')

-# 銘柄コード検索フォーム
= render 'shared/ts_search'

-# セクターをリスト表示
= render 'sector_list'

-# 企業一覧を表示
= render 'companies/company_list'

-# 企業一覧インポート機能
-# = form_tag import_from_companies_path, multipart: true do
-#   = file_field_tag :file
  -# = submit_tag "インポート"



