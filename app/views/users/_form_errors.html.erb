
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>以下の項目をご確認の上、再度実行してください</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>