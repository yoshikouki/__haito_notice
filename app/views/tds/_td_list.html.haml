-# TDnet情報をリスト表示
-# tds_hash()から取得

-# TDnetの戻り値は、複数の場合と単数の場合で配列階層が異なる
%section.list.border-top
  - if @tds.count >= 2 then
    -# TD情報が（戻り値）が複数の場合
    = render partial: 'td', collection: @tds

  - elsif @tds.count == 1
    -# TD情報が一つだった場合
    = render partial: 'td', td: @tds

  - else
    -# TD情報がなかった場合
    = render 'empty_td'